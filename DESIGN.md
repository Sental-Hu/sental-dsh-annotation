# dsh-annotation 架构

当前版本：0.1.17。操作和安装方法见 README.md。

## 场景和边界

用户在 AI 已完成的普通文本回答中划选原文，写下针对性回复。点击“保存”保留批注并将新批注引用加入输入框，供集中回应；点击“发送”保存并单独发送当前批注，成功后列入输入框上方的历史批注。直接发送保留输入框已有草稿和引用。

插件不替换 DSH 消息渲染器，不修改 DSH 核心源码。代码、表格、工具结果和跨消息选区不支持批注。同一回答内相邻普通段落可跨段选择。批注编号在删除其他条目后保持稳定。

## 模块

- `src/client/`：选区、高亮、批注弹窗、历史列表、输入引用及直接发送。
- `src/compat/`：DSH 会话订阅、输入接口和持久化接口适配。
- `src/service.ts`、`src/repository.ts`：批注存储、版本冲突和发送批次。
- `src/http.ts`：同源 API、请求校验和插件生命周期。
- `src/history.ts`：已确认批注的 Markdown 历史投影。
- `src/shared/`：数据结构、锚点、颜色和发送格式。

服务端和浏览器端打包在同一个插件中。`cordis.patch.yml` 声明插件组合；浏览器构建使用 DSH 模块加载器，由宿主提供 React。

## 发送和持久化

结构化批注保存在 `dsh_annotation` sidecar domain。批注状态为 pending、prepared、unknown 或 sent。准备批次固定成员及内容，发送后依据持久会话记录确认 sent。界面调用返回、超时和内存状态均不能单独证明发送成功。

直接发送通过公开的 `conversation.send` 接口执行；集中发送由输入引用序列化器接入 DSH 提交过程。结果不明确时保留批注，允许核对已有记录，禁止自动重发。版本号防止多个页面静默覆盖彼此的修改。

发送正文包含原文和批注。不可见批次标记用于关联持久记录，兼容旧格式识别。用户可写工作区中的 `.dsh/annotations/` 保存可读历史投影，结构化存储仍是批注状态的依据。

## 兼容和验证

普通文本通过 `data-chat-anchor-key` 与已完成的 assistant 消息关联。DOM 锚点与公开会话接口是需要持续验证的宿主边界。持久化适配支持旧版 inspect/readFrom 和新版 open/read/close。

自动测试覆盖选区、锚点、版本冲突、保存与直接发送、草稿保护、批次幂等、持久化确认、HTTP 校验及打包契约。浏览器实际操作与自动测试分别验证；完整操作系统支持范围见 README.md。
